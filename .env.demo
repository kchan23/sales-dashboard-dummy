DEMO_MODE=true
# No GCP or Toast credentials needed in demo mode.
# Run: python scripts/generate_demo_data.py  (once, to create demo_data/*.parquet)
# Then: DEMO_MODE=true streamlit run app.py
#   or: set -a && source .env.demo && set +a && streamlit run app.py
